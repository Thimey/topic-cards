import React from 'react';

const CardCompleteButton = ({stroke, fill}) => (
    <svg>
        <g>
            <rect x="50" y="20" width="150" height="150"/>
        </g>
    </svg>
);

export default CardCompleteButton;
